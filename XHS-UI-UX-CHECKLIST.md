# 小红书风格 UI/UX 验收标准（xhs-studio）

改 `frontend/**`、`page.css`、主题或发布流程前，**逐项勾选**。模板侧摘要见 `../xhs-templates/CHECKLIST.md`。Cursor 规则：`.cursor/rules/xhs-studio-ui-ux.mdc`。

---

## 1. 画布与导出

- [x] 单页逻辑尺寸严格 **1080 × 1440**（**3:4** 竖版）
- [x] 预览只用 `transform: scale()`，**不改**导出像素
- [x] 导出：`width/height = 1080/1440`，`pixelRatio = 倍率`（默认 **2× → 2160×2880**）
- [x] **禁止**把 `width/height` 再乘倍率（双重放大）
- [x] 导出前 `await document.fonts.ready`

## 2. 字号 / 边距 / 信息流扫读

- [x] 大字号优先：封面标题约 **90–100px**，块标题约 **50–60px**，正文约 **24–28px**（以 `page.css` 为准，勿回退到「文档字号」）
- [x] 页边距约 **64 / 56 / 56**；安全区 ≥ **48px**；compact ≥ **52px**
- [x] 列表等高撑满：points/timeline/summary 用 `flex:1` 均分高度（非 space-between 飞空白）；字号加大一档；预览 scale 占满右侧可视区
- [x] 信息流扫读：一眼能抓住标题与结论；次要信息（footer、kicker）不抢戏

## 3. 封面 Hook 与跨页叙事

- [ ] 封面有强 **hook**（痛点 / 结果 / 反差），标题可换行，副标短（依赖生成质量，未固定验收）
- [ ] 页序有逻辑：封面 →（章节）→ 干货页 → 对比/总结 → 结尾 CTA（schema/repair 有约束，内容质量未全量抽检）
- [ ] 页间用章节页或标题承接做**软引导**，勿突然跳主题
- [x] 结尾有明确行动（关注 / 收藏 / 私信等文案，不用 emoji）

## 4. 对比页与卡片（非 Excel）

- [x] compare 是**对比卡片**观感：色块表头、维度列强调、无全格线表格感
- [x] 避免 Excel 网格、等宽细线满屏、密集小字单元格
- [x] 要点 / 总结用块状卡片，而非清单 bullet 堆砌

## 5. 图文混排（传图）

- [x] 封面可叠图 + 遮罩，标题仍清晰可读
- [x] photo / gallery 图大字少；图块圆角与主题一致，不贴边裁切
- [x] 图文层级：图为视觉锚点，文案短句补充，勿图下再塞长文

## 6. 文案与创作者语感

- [x] **不用 emoji**；列表用 `>`、`*`、`>>` 等字符
- [ ] 中文创作者口吻：干货、真诚、可收藏；忌公文 / 后台术语堆砌（生成质量波动，未本轮签字）
- [x] 标题与正文支持 `\n` → 换行；超高显示「内容溢出 · 请精简」

## 7. 产品流与发布包

- [x] 主路径：**写意图 → 出稿审核（①写稿/②审核）→ 改页导出**（3 主步；与「生成并审核」+ 2+1 对齐）
- [x] 审核 `pass` 或翻完全部页后默认可改页/导出；`warn` 需确认继续；`fail` 须先改
- [x] `confirmLayout` **不**无条件清除 `needsRereview`（审核阶段改标题/正文/hooks 标记保留）
- [x] 发布包三件套齐全：`pages/*.png` + `title.txt` + `caption.txt`（及 `meta.json`）
- [x] 导出按钮仅在确认排版后高亮可点；需复审时内联三选一（先复审 / 直接导出 / 取消），无双 `window.confirm`
- [x] Demo / 降级仅顶部一条黄条（header 不重复 mode pill）
- [x] 单页 AI 改可撤销（切页后显示「撤销第 N 页」）；写稿等待右侧 3:4 骨架 + 已用/预估

## 8. Studio 壳层（创作者工具，非后台表单）

- [x] 步骤条清晰：3 主步 + 子状态（①写稿/②审核）；阶段文案 + elapsed + 预估；无假百分比
- [x] 主按钮层级明确：每步一个主 CTA；审核 warn 时吸顶「确认继续」；只读工具栏「开始改这一页」
- [x] 审核面板可扫读：结论 + 问题 + 次级操作；hooks 在标题旁
- [x] 结构大纲默认展开（`review-extra` 带 `open`）
- [x] 预览区像**手机竖屏卡片**（圆角、阴影、居中），不是后台嵌 iframe
- [x] 进入审核后意图区收成摘要条；高级项折叠
- [x] 壳层干净：少 pill 堆砌、少技术 jargon（`type` 用中文标签）
- [x] 七主题默认仅推荐卡 +「换风格」展开（`themeExpanded` 默认 false）
- [x] 手机：全屏预览入口，避免侧栏挤死预览
- [x] 「改这一页」：短输入 +「改」；按 type 动态芯片（改字/版式）；空指令=智能优化；成功一行人话反馈；溢出「变紧凑」0 LLM density

## 10. AI native 增强控制（2026）

- [x] 手工与 AI 共用 **ops**（`ops.js` / `ops.py`）；禁止 DOM→CSS 旁路
- [x] 不调 AI 也能：点改字、列表拖拽、复制/删/加页、换图、导出
- [x] IndexedDB 草稿自动保存；生成失败保留旧稿；改页失败保留 undo
- [x] 导出前 title/caption + 逐页 overflow 闸口；直接导出须风险确认
- [x] 移动端首屏可见「生成并审核」；`viewport-fit=cover` + safe-area
- [x] 会话 LLM 累计 pill；layout-ideas 一级入口 + 预览确认后再应用
- [x] `aria-live` 状态、`prefers-reduced-motion`、触控 ≥44px

## 9. 反模式（出现即不合格）

- [x] 小字密排、大留白、像 Word/Excel（对照检查：导出/预览路径无双重放大）
- [x] footer / 边框线过重盖过正文（主题侧已压；未再回归）
- [x] 双重放大导出、非 3:4 画布
- [x] emoji 装饰、英文全大写后台标签刷屏（页类型用中文）
- [x] 跳过审核直接导出、发布包缺 title/caption

---

## 快速自检命令

```powershell
# 启动后打开 http://127.0.0.1:8765/ ，Ctrl+F5 强刷
# 走完：生成 → 审时翻页看图 →（pass 自动可改）→ 芯片/改 → 撤销 → 导出 ZIP
# 解压确认 PNG 为 2160×2880（2×）或 1080×1440（1×）
```
